name: My First GitHub App

on: [push, issues] # แอปจะทำงานเมื่อมีการ Push Code หรือสร้าง Issue

jobs:
  run-app:
    runs-on: ubuntu-latest # ใช้เครื่อง Linux ของ GitHub รันให้ฟรี
    steps:
      - name: Check out code
        uses: actions/checkout@v3

      - name: Run Greeting Script
        run: |
          echo "🚀 แอปกำลังเริ่มทำงานบน GitHub Cloud..."
          echo "สวัสดี! ยินดีต้อนรับสู่แอปแรกบน GitHub ของฉัน"
          echo "เวลาปัจจุบันที่เครื่องรันคือ: $(date)"
